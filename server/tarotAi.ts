import { generateText } from "./_core/aiProvider";
import {
  MAX_CHOICE_OPTIONS,
  MIN_CHOICE_OPTIONS,
  THREE_SPREAD,
  normalizeSpreadChoice,
  optionLetter,
  spreadPlan,
  type SpreadChoice,
} from "@shared/tarot";

export interface TarotAiCard {
  /** שם אפקטיבי (override מה-DB או ברירת המחדל מ-shared). */
  name: string;
  /** שורת מהות קצרה מה-DB (יכולה להיות ריקה). */
  summary: string;
  /** הפירוש הסטטי כ-plain text (הקליינט ממיר מ-HTML). */
  text: string;
}

export interface TarotAiContext {
  /** יכולה להיות ריקה — שליפה בלי שאלה מותרת (קריאה כללית). */
  question: string;
  /** בסדר השליפה; האורך תואם לתוכנית הפריסה (3 ל-three, 6/8/10 ל-choice). */
  cards: TarotAiCard[];
  /** הפריסה שנפרסה (ברירת מחדל: three). קובעת את שיטת הקריאה בפרומפט. */
  spread?: SpreadChoice;
}

/** "שם — מהות — פירוש", בלי מקפים תלויים כשחלקים ריקים. */
function cardText(card: TarotAiCard): string {
  return [card.name, card.summary.trim(), card.text.trim()].filter((s) => s.length > 0).join(" — ");
}

/** נקודת הכניסה: מסתעפת לפי הפריסה. three = השיטה הקיימת (מרכזי + מסייעים). */
export function buildTarotPrompt(c: TarotAiContext): string {
  return c.spread?.kind === "choice" ? buildChoicePrompt(c, c.spread) : buildThreePrompt(c);
}

/**
 * בונה את פרומפט קריאת הפריסה. טהורה — בלי רשת, בלי env.
 * שיטת הקריאה (החלטת 2026-09-10, מחליפה את "העמדות החופשיות" מ-2026-09-09):
 * הקלף האמצעי (השני בשליפה, המוצג במרכז) נושא את התשובה עצמה; שני הצדדיים
 * מסייעים — או כשני פנים משלימים, או כתומך ומתנגד. ברוח "דרך הרוח":
 * נטיות, לא הגדת עתידות.
 */
function buildThreePrompt(c: TarotAiContext): string {
  const question = c.question.trim();
  const questionLine = question
    ? `שאלת המשתמש: "${question}"`
    : `המשתמש שלף בלי שאלה — זו קריאה כללית: מראה למצבו הפנימי בתקופה זו.`;

  const roles = [
    "קלף 1 (מסייע, לצד המרכזי)",
    "קלף 2 (הקלף המרכזי — נושא התשובה)",
    "קלף 3 (מסייע, לצד המרכזי)",
  ];
  const cardBlock = c.cards.map((card, i) => {
    const summary = card.summary.trim();
    const text = card.text.trim();
    const parts = [card.name, summary, text].filter((s) => s.length > 0);
    return `${roles[i]}: "${parts.join(" — ")}"`;
  });

  return [
    `את/ה קורא/ת טארוט ותיק/ה הפועל/ת ברוח "דרך הרוח": הקלפים הם מראה סמלית להבנת נטיות וכיוונים במצבו של השואל — לא הגדת עתידות ולא קביעת ודאות. הפירוש מתווך בשכל ישר, פסיכולוגי ומעשי, בגובה העיניים ובלי מחמאות מיותרות.`,
    ``,
    questionLine,
    ``,
    ...cardBlock,
    ``,
    `שיטת הקריאה — קלף מרכזי ושני מסייעים:`,
    `הקלף המרכזי (קלף 2) הוא לב הקריאה: בו נמצאת התשובה לשאלה. שני הקלפים שלצדדיו אינם תשובות נוספות — הם משרתים את הבנת המרכזי. קרא/י אותם באחד משני האופנים, לפי מה שהקלפים שנשלפו באמת מציעים:`,
    `א. **שני מסייעים** — כל צדדי מאיר פן אחר של התשובה: מאין היא צומחת, מה מזין אותה, איך מממשים אותה, או באיזה תחום היא מתבטאת.`,
    `ב. **תומך ומתנגד** — צדדי אחד מחזק את כיוון התשובה ומראה מה פועל לטובתה, והשני מציג את הכוח הפועל נגדה: המכשול, המחיר או הסייג שאסור להתעלם ממנו.`,
    `כלל הכרעה: אם אחד הצדדיים סותר במהותו את רוח הקלף המרכזי — קרא/י באופן ב', והסותר הוא המתנגד; אל תרכך/י את הסתירה, היא לב המידע. אם שניהם מתיישבים עם המרכזי — קרא/י באופן א'. ציין/ני במשפט אחד באיזה אופן קראת ולמה.`,
    ``,
    `סדר עבודה פנימי — עבור/עברי עליו לפני כתיבת התשובה:`,
    `1. הבן/י את השאלה: מה באמת נשאל (הכרעה בין אפשרויות? בקשת כיוון? הבנת מצב?), ומה ייחשב בעיני השואל תשובה אמיתית ולא התחמקות. בקריאה כללית: השאלה היא "מה מצבי הפנימי עכשיו ומה מבקש תשומת לב".`,
    `2. זקק/י את מהות הקלף המרכזי למשפט אחד — ואז תרגמ/י אותה אל תחום השאלה עצמו. זו ליבת התשובה. אם הקלף המרכזי קשה או מאתגר — זו התשובה; אל תחליף/י אותה בנעימה יותר מהצדדיים.`,
    `3. קבע/י לכל קלף צדדי את יחסו אל המרכזי: מחזק, סותר, מסייג או מוסיף תנאי — ומכאן בחר/י את אופן הקריאה (א' או ב').`,
    `4. נסח/י את התשובה כך שהיא עונה על השאלה שנשאלה — לא פירוש כללי של קלפים. שאלת הכרעה מקבלת נטייה ברורה לכיוון אחד (עם הסייג של המתנגד, אם יש); שאלת מצב מקבלת תמונה ומה לעשות בה.`,
    `5. בדיקה עצמית לפני מסירה: האם שורת המהות עונה ישירות על השאלה? האם כל קלף נקרא בהקשר השאלה ולא כפירוש גנרי? האם השואל יודע כעת מה לעשות מחר בבוקר? אם לא — תקנ/י לפני שתשיב/י.`,
    ``,
    `מבנה התשובה:`,
    `1. **שורת מהות** בראש התשובה — התשובה לשאלה במשפט אחד, הנשען על הקלף המרכזי, פותחת במילים מודגשות.`,
    `2. משפט אחד: באיזה אופן נקראו המסייעים (שני פנים / תומך ומתנגד) ולמה.`,
    `3. **הקלף המרכזי** — קריאתו בהקשר השאלה: מדוע זו התשובה ומה היא אומרת בפועל.`,
    `4. שני המסייעים, כל אחד ביחסו למרכזי — מה הוא מוסיף, מחזק או מסייג. לא פירוש עצמאי מנותק.`,
    `5. **חיבור** — התמונה השלמה: התשובה על סייגיה, המתח או התנועה שבין השלושה.`,
    `6. **דרך פעולה** — 2-3 נקודות מעשיות שהשואל יכול לקחת איתו.`,
    ``,
    `הסתמך/י על הטקסטים שסופקו כבסיס לשיפוט בלבד — אל תצטט/י אותם ואל תיצמד/י לאוצר־המילים שלהם. תרגמ/י את המשמעות לשפה ישירה הקשורה לשאלה.`,
    `אל תבטיח/י ודאות ואל תנבא/י עובדות עתידיות — דבר/י על נטיות, תנועות ואפשרויות פעולה שבידי השואל.`,
    `החזר/י את התשובה בעברית, בפורמט נקי ונוח לקריאה (Markdown), עם כותרות קצרות ונקודות (bullet points).`,
  ].join("\n");
}

/**
 * פרומפט לפריסת בחירה: הצומת → לכל דרך "מה מציעה" / "לאן מובילה" → "מה שאינך רואה".
 * הקריאה משווה את הדרכים באותם תפקידים; ההכרעה נשארת אצל השואל. טהורה.
 */
function buildChoicePrompt(c: TarotAiContext, spread: SpreadChoice): string {
  const plan = spreadPlan(spread);
  const question = c.question.trim();
  const optionsLine = spread.options.map((o, i) => `דרך ${optionLetter(i)}: "${o}"`).join(" · ");
  const cardBlock = c.cards.map((card, i) => {
    const pos = plan.positions[i];
    const role = pos ? pos.role : `קלף ${i + 1}`;
    return `קלף ${i + 1} — ${role}: "${cardText(card)}"`;
  });
  const optionSections = spread.options
    .map((o, i) => `   - **דרך ${optionLetter(i)} — ${o}**: מה היא מציעה ולאן היא מובילה, כזוג — המשיכה מול המחיר.`)
    .join("\n");

  return [
    `את/ה קורא/ת טארוט ותיק/ה הפועל/ת ברוח "דרך הרוח": הקלפים הם מראה סמלית להבנת נטיות וכיוונים במצבו של השואל — לא הגדת עתידות ולא קביעת ודאות. הפירוש מתווך בשכל ישר, פסיכולוגי ומעשי, בגובה העיניים ובלי מחמאות מיותרות.`,
    ``,
    `שאלת המשתמש: "${question}"`,
    `זו שאלת בחירה, ונפרסה לה **${plan.title}** בין ${spread.options.length} דרכים — ${optionsLine}.`,
    ``,
    ...cardBlock,
    ``,
    `שיטת הקריאה — השוואת דרכים באותם תפקידים:`,
    `- **הצומת** (קלף 1) מתאר מאיזה מקום השואל בוחר: מה מביא אותו לצומת, מה הוא מחפש ומה הוא נושא איתו. קרא/י אותו קודם — הוא הרקע לכל השאר.`,
    `- לכל דרך שני קלפים באותם תפקידים: **מה היא מציעה** (הצד הגלוי, מה מושך אליה, מה היא נותנת) ו**לאן היא מובילה** (התוצאה הסבירה, כולל המחיר והדרישה). קרא/י כל דרך כזוג, ואז השווה/י בין הדרכים תפקיד מול תפקיד — הצעה מול הצעה, תוצאה מול תוצאה. אין להשוות "הצעה" של דרך אחת ל"תוצאה" של אחרת.`,
    `- **מה שאינך רואה** (הקלף האחרון) הוא עצה, גורם נסתר, או דרך שלישית שלא עלתה בשאלה. אם הוא סותר את רוח שתי הדרכים — אמור/אמרי זאת במפורש: ייתכן שהשאלה עצמה מנוסחת לא נכון, או שהתשובה נמצאת מחוץ לצומת.`,
    `- הקלפים מתארים, הם לא מכריעים. מותר ורצוי לציין לאן הם **נוטים** — אבל ההכרעה נשארת בידי השואל, ואין לומר לו "בחר ב-X".`,
    ``,
    `סדר עבודה פנימי — עבור/עברי עליו לפני כתיבת התשובה:`,
    `1. זקק/י את הצומת למשפט אחד: מאיפה השואל בוחר.`,
    `2. לכל דרך: משפט על מה שהיא מציעה, משפט על לאן היא מובילה, ומשפט על היחס ביניהם (האם ההבטחה מתממשת? מה המחיר?).`,
    `3. השווה/י: איזו דרך הקלפים מציגים כתואמת יותר את מה שהשואל באמת מחפש (לפי הצומת) — ובאיזה סייג.`,
    `4. קרא/י את "מה שאינך רואה" מול המסקנה: מחזק, מסייג, או פותח דרך אחרת.`,
    `5. בדיקה עצמית: האם כל דרך נקראה בהקשר השאלה ולא כפירוש גנרי? האם ההשוואה היא תפקיד מול תפקיד? האם השואל יודע כעת מה לבדוק או לעשות מחר בבוקר? אם לא — תקנ/י.`,
    ``,
    `מבנה התשובה:`,
    `1. **שורת מהות** בראש התשובה — לאן הקלפים נוטים ובאיזה סייג, במשפט אחד, פותח במילים מודגשות.`,
    `2. **הצומת** — מאיזה מקום נבחרת הבחירה.`,
    `3. סעיף לכל דרך, בסדר שבו נשאלו:`,
    optionSections,
    `4. **מה שאינך רואה** — העצה / הגורם הנסתר / הדרך השלישית, ויחסו למסקנה.`,
    `5. **חיבור** — ההשוואה השלמה: מה מבדיל בין הדרכים באמת, ומה מכריע בעיני הקלפים.`,
    `6. **דרך פעולה** — 2-3 נקודות מעשיות: מה לבדוק, לברר או לעשות לפני ההכרעה ואחריה.`,
    ``,
    `הסתמך/י על הטקסטים שסופקו כבסיס לשיפוט בלבד — אל תצטט/י אותם ואל תיצמד/י לאוצר־המילים שלהם. תרגמ/י את המשמעות לשפה ישירה הקשורה לשאלה.`,
    `אל תבטיח/י ודאות ואל תנבא/י עובדות עתידיות — דבר/י על נטיות, תנועות ואפשרויות פעולה שבידי השואל.`,
    `החזר/י את התשובה בעברית, בפורמט נקי ונוח לקריאה (Markdown), עם כותרות קצרות ונקודות (bullet points).`,
  ].join("\n");
}

/**
 * מייצר פירוש פריסה (Markdown) דרך הספק המשותף. זורק אם אין מפתח / אם הקריאה
 * נכשלה; שגיאות חולפות עוברות ניסיון חוזר בשכבת הספק.
 */
export async function generateTarotInterpretation(c: TarotAiContext): Promise<string> {
  // פריסת בחירה (6–10 קלפים, סעיף לכל דרך) צריכה תקציב תשובה גדול יותר משלושה קלפים.
  const maxTokens = c.spread?.kind === "choice" ? 5000 : 3000;
  return generateText(buildTarotPrompt(c), { maxTokens });
}

// ── בחירת הפריסה (לפני השליפה): ה-AI מסווג את השאלה ובוחר מהקטלוג ──

/**
 * פרומפט סיווג קצר: האם השאלה היא הכרעה בין אפשרויות מפורשות (פריסת בחירה) או
 * שאלת מצב/כיוון/הבנה (שלושה קלפים). טהורה — בלי רשת, בלי env.
 */
export function buildSpreadChoicePrompt(question: string): string {
  return [
    `את/ה קורא/ת טארוט. לפניך שאלה של שואל, ועליך לבחור את הפריסה המתאימה לה מתוך הקטלוג — בלי לפרש ובלי לענות על השאלה.`,
    ``,
    `שאלת השואל: "${question}"`,
    ``,
    `הקטלוג:`,
    `- "three" — פריסת שלושה קלפים (ברירת המחדל): שאלת מצב, כיוון, הבנה, תהליך, "מה נכון להבין / איך לפעול / מה מתפתח", וגם כל שאלה עמומה או כללית.`,
    `- "choice" — פריסת בחירה: השואל מתלבט במפורש בין ${MIN_CHOICE_OPTIONS} עד ${MAX_CHOICE_OPTIONS} אפשרויות מוגדרות ("לעבור לתל אביב או להישאר בירושלים", "איזה מסלול לבחור: הנדסה, רפואה או משפטים", "להישאר בעבודה, לעבור לחברה החדשה או לפתוח עסק").`,
    ``,
    `כללי הכרעה:`,
    `- "X או לא?" / "האם לעשות X?" היא שאלת כן/לא על דרך אחת — לא בחירה בין דרכים → "three".`,
    `- "מה עדיף" בלי אפשרויות שאפשר לנסח — → "three".`,
    `- יותר מ-${MAX_CHOICE_OPTIONS} אפשרויות → "three".`,
    `- בספק — "three".`,
    ``,
    `ב-"choice": נסח/י כל אפשרות בקצרה (עד 8 מילים, בעברית, כפי שהשואל התכוון, בסדר שבו הזכיר אותן), למשל ["לעבור לתל אביב", "להישאר בירושלים"].`,
    ``,
    `בנוסף, לשני הסוגים: "title" — כותרת קצרה לקריאה (2–6 מילים בעברית) שמסכמת את נושא השאלה, מתאימה לשם קובץ: בלי סימני פיסוק מיוחדים, בלי סימן שאלה, בלי "שאלה על". למשל: "קטלבל הרד סטייל או ספורט", "מעבר דירה לתל אביב", "הכיוון בתקופה הזו".`,
    ``,
    `החזר/י JSON תקין בלבד, ללא טקסט נוסף וללא סימוני קוד:`,
    `{"kind": "three" | "choice", "options": string[], "title": string}`,
    `כאשר kind="three" החזר/י options ריק ([]).`,
  ].join("\n");
}

/**
 * בוחר פריסה לשאלה דרך ה-AI. Fail-open לחלוטין: שגיאה / JSON פגום / בחירה לא תקינה
 * → THREE_SPREAD. הפונקציה לעולם לא זורקת — "שלושה קלפים" הוא תמיד מוצא בטוח.
 */
export async function chooseTarotSpread(question: string): Promise<SpreadChoice> {
  try {
    const text = await generateText(buildSpreadChoicePrompt(question), { maxTokens: 300 });
    const match = text.match(/\{[\s\S]*\}/);
    if (!match) {
      console.warn("[tarot] chooseSpread: no JSON in reply", text.slice(0, 200));
      return THREE_SPREAD;
    }
    return normalizeSpreadChoice(JSON.parse(match[0]));
  } catch (err) {
    // Fail-open, אבל משאירים עקבות בלוג — אחרת אי אפשר להבין למה נפרסו שלושה קלפים.
    console.warn("[tarot] chooseSpread failed:", err instanceof Error ? err.message : err);
    return THREE_SPREAD;
  }
}
